# تحليل شامل لتفاصيل مشروع Smart Traffic Management

## 1. نظرة عامة على النظام (System Overview)
المشروع عبارة عن منصة متكاملة (Full-Stack) تجمع بين **أنظمة المرور الذكية والطوارئ** وبين **خدمات العناية بالسيارات والتجارة الإلكترونية (AutoCare)**. 
يتكون النظام من ثلاثة أنظمة فرعية (Subsystems) مترابطة ببعضها لتوفير تجربة شاملة:
1. **الخادم الخلفي (Backend API):** العقل المدبر للنظام والمزود المركزي للبيانات (Single Source of Truth).
2. **تطبيق الهاتف المحمول (Mobile App):** موجه أساساً للمستخدمين النهائيين (السائقين للتسوق، وطلب الطوارئ) وللميكانيكية.
3. **تطبيق الويب (Web Dashboard):** لوحات تحكم متعددة الأدوار (إدارة عليا، بائعين، مزودي خدمات، وخدمة عملاء).

---

## 2. تفاصيل الخادم الخلفي (Backend - .NET 8.0)
يقع الخادم في مجلد `src/`، وتم تصميمه بنمط **البيئة النظيفة (Clean Architecture)** لضمان قابلية التوسع والصيانة.

### أ. الطبقات الهندسية (Layers):
1. **طبقة العرض (SmartTrafficManagement.API):**
   - تعمل كنقطة الدخول (Entry Point) للتطبيق.
   - تحتوي على أكثر من 20 `Controller` مقسمة حسب الوظائف: `AdminController`, `AuthController`, `PaymentsController`, `TrafficIncidentsController`, `SellerController` وغيرها.
   - تتولى إدارة صلاحيات الوصول (JWT Authorization) بناءً على الأدوار (Roles).
2. **طبقة التطبيق (SmartTrafficManagement.Application):**
   - تضم العمليات المنطقية (Business Logic).
   - مقسمة إلى مجلد `Features` يشمل: Auth, Cart, Chat, Garage, Orders, Sos, TrafficIncidents, وغيرها.
   - مسؤولة عن التحقق من صحة البيانات (Validators) ونقلها (DTOs).
3. **الطبقة الأساسية (SmartTrafficManagement.Core):**
   - تحتوي على الجداول الأساسية للقاعدة (Entities) مثل: `ApplicationUser`, `Product`, `TrafficIncident`, `ServiceRequest`, `Order`.
   - تضم واجهات العقود (Interfaces).
4. **طبقة البنية التحتية (SmartTrafficManagement.Infrastructure):**
   - تنفيذ العمليات الخاصة بقاعدة البيانات (Entity Framework Core) والتواصل مع `SQL Server`.
   - تحتوي على تكاملات خارجية قوية مثل: بوابات دفع **Stripe.net** لمعالجة البطاقات، ومكتبة **ImageSharp** لرفع وتحسين صور الحوادث والمنتجات.

---

## 3. تفاصيل تطبيق الهاتف المحمول (Mobile App - Flutter)
يقع التطبيق في مجلد `autocare_app/`، ومبني بالكامل باستخدام إطار عمل **Flutter (Dart)**، وهو موجه لتوفير تجربة مستخدم سلسلة للسائقين ومقدمي الخدمات.

### أ. التقنيات وإدارة الحالة:
- **State Management:** يعتمد التطبيق على مكتبة **Riverpod** (`flutter_riverpod`) لإدارة حالة التطبيق بشكل تفاعلي وفعال.
- **الخرائط والملاحة (Maps):** دمج عميق لمكتبات الخرائط (`flutter_map`) والملاحة (`geolocator`, `geocoding`) لتحديد مواقع المستخدمين، وعرض الحوادث المرورية على الخريطة في الوقت الفعلي.
- **المدفوعات الإلكترونية:** دمج `flutter_stripe` للدفع من داخل التطبيق بأمان.

### ب. هيكلية الميزات (Features in `lib/features/`):
- **Store & Checkout:** واجهات لتصفح المتجر، إضافة قطع الغيار للسلة، وإتمام عمليات الشراء.
- **Emergency & Traffic:** شاشات لتقديم تقارير عن الحوادث المرورية، وطلب التدخل السريع (SOS) بنقرة واحدة مع تحديد الموقع الجغرافي.
- **Maintenance & Mechanic:** حجز طلبات الصيانة، والتواصل مع الورش القريبة.
- **History & Reports:** تتبع سجل العمليات الشرائية، وصيانة السيارة.
- **Chat:** نظام محادثة مدمج للتواصل مع فريق خدمة العملاء (CS) أو مزودي الخدمة مباشرةً.

---

## 4. تفاصيل تطبيق الويب (Web App - React)
يقع في مجلد `my-project/my-project/`، وهو تطبيق ويب حديث مبني باستخدام **React 19** وأداة البناء **Vite**، مع واجهات مصممة باستخدام **TailwindCSS**. 

### أ. الهدف والتصميم:
التطبيق مصمم ليكون **لوحة تحكم مركزية (Dashboards)** لإدارة أجزاء النظام المختلفة بناءً على صلاحيات المستخدم، مما يسهل سير العمل دون الحاجة للتدخل في قاعدة البيانات مباشرة.

### ب. لوحات التحكم والشاشات (Pages in `src/pages/`):
- **Landing.jsx:** الصفحة التسويقية أو الترحيبية للمنصة.
- **Admin.jsx (لوحة الإدارة):** مخصصة لمديري النظام (Admins) لمراقبة المستخدمين، مراجعة التقارير، التحكم في صلاحيات النظام بالكامل.
- **Seller.jsx (لوحة البائعين):** منصة للتجار لعرض منتجاتهم (قطع الغيار)، إدارة التصنيفات، متابعة المبيعات والطلبات الواردة من تطبيق الموبايل.
- **Provider.jsx (لوحة مزودي الخدمات):** مخصصة لأصحاب الورش لقبول طلبات الصيانة (Service Requests) الواردة من السائقين، وتحديث حالة العمل.
- **CsAgent.jsx (لوحة خدمة العملاء):** واجهة مخصصة لموظفي الدعم الفني للرد على تذاكر الدعم (Support Tickets) والتواصل المباشر مع السائقين والمستخدمين.

---

## 5. كيف تترابط الأنظمة الثلاثة معاً (System Flow Example)
1. **الحدث (عبر الموبايل):** السائق تعطلت سيارته، يقوم بفتح تطبيق Flutter ويرسل طلب طوارئ (SOS) أو طلب صيانة مع إرفاق موقعه الجغرافي.
2. **المعالجة (عبر الباك إند):** الـ API يستقبل الطلب ويحفظه في قاعدة البيانات (`ServiceRequest`)، ثم يقوم بتوليد إشعارات (Notifications) وتوجيهها في الوقت الفعلي.
3. **الاستجابة (عبر الويب):** يظهر الطلب فوراً في لوحة تحكم مزود الخدمة القريب (`Provider.jsx`)، حيث يمكنه قبول الطلب. وإذا كان هناك مشكلة تقنية، يمكن للسائق التواصل عبر الشات الذي يظهر فوراً لموظف الدعم الفني في لوحته (`CsAgent.jsx`).
4. **الدفع والتجارة:** إذا قرر السائق شراء قطعة غيار للمحرك، يتم الطلب والدفع عبر המوبايل (`flutter_stripe`)، ويقوم الباك إند بالتحقق والتأكيد (`Stripe.net`)، ثم يظهر إشعار بطلب جديد في لوحة تحكم التاجر (`Seller.jsx`) لتجهيز الشحنة.
